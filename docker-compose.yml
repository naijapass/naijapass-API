# version: '3.8'

# services:
#   backend:
#     build:
#       context: .
#       dockerfile: Dockerfile
#     container_name: naijapass-backend
#     restart: always
#     expose:
#       - "7227"
#     environment:
#       - NODE_ENV=production
    

#   nginx:
#     image: nginx:alpine
#     container_name: naijapass-nginx
#     restart: always
#     ports:
#       - "80:80"
#       - "443:443"
#     volumes:
#       - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
#       - /etc/letsencrypt:/etc/letsencrypt:ro
#     depends_on:
#       - backend

# volumes:
#   naijapass_mongo_data:

version: '3.8'
services:
  backend:
    build: .
    container_name: naijapass-backend
    restart: always
    expose:
      - "7227"
    networks:
      - naijapass_network

  nginx:
    image: nginx:alpine
    container_name: naijapass-nginx
    restart: always
    expose:
      - "80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - naijapass_network

networks:
  naijapass_network:
    external: true